package com.mediscreen.central.controller;

/*
@ExtendWith(SpringExtension.class)
@WebMvcTest(DiabetesRiskController.class)
@ContextConfiguration(classes = CentralApplication.class)
public class DiabetesRiskControllerTest {

    @Autowired
    MockMvc mvc;

    @MockBean
    PatientDiabetesRiskProxy patientDiabetesRiskProxy;

    @MockBean
    PatientClientProxy patientClientProxy;

    @MockBean
    PatientNotesClientProxy patientNotesClientProxy;

    @Test
    public void calculateDiseaseRickByIdViewTest () throws Exception {
        mvc.perform(get("/central/calculate-disease-risk-by-id/").param("patId", asJsonString(1L)))
                .andExpect(status().isOk());
    }

    public static String asJsonString(final Object obj) {
        try {
            return new ObjectMapper().writeValueAsString(obj);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
    }

} */
